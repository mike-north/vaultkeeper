<!-- Do not edit this file. It is automatically generated by API Documenter. -->

[Home](./index.md) &gt; [@vaultkeeper/wasm](./wasm.md)

## wasm package

@<!-- -->vaultkeeper/wasm — WASM-backed vaultkeeper SDK for Node.js.

Uses the Rust vaultkeeper-core compiled to WebAssembly, with Node.js providing the host platform (file I/O, subprocess execution).

## Example


```ts
import { createVaultKeeper } from '@vaultkeeper/wasm';

const vault = await createVaultKeeper({ skipDoctor: true });
const token = vault.setup('my-secret', 'secret-value');
const { claims, secret } = vault.authorize(token);
// `claims` never contains the raw secret; read it once via the accessor:
const first4 = secret.read((value) => value.slice(0, 4));
```

## Classes

<table><thead><tr><th>

Class


</th><th>

Description


</th></tr></thead>
<tbody><tr><td>

[AccessorConsumedError](./wasm.accessorconsumederror.md)


</td><td>

Thrown when a one-time secret accessor's `read()` is called after the secret has already been consumed.


</td></tr>
<tr><td>

[DecryptionError](./wasm.decryptionerror.md)


</td><td>

Thrown when a stored secret entry cannot be decrypted — the ciphertext is corrupted/truncated or the AES-GCM authentication tag failed to verify. Mirrors the pure-TypeScript `vaultkeeper` library's `DecryptionError`<!-- -->.


</td></tr>
<tr><td>

[InvalidTokenError](./wasm.invalidtokenerror.md)


</td><td>

Thrown when a JWE string is invalid or cannot be processed — structurally malformed, decryption failure (wrong key, tampered ciphertext), or a decrypted payload that does not match the expected claims schema.


</td></tr>
<tr><td>

[KeyRevokedError](./wasm.keyrevokederror.md)


</td><td>

Thrown when the encryption key referenced by a JWE's `kid` header has been explicitly revoked and is no longer available for decryption.


</td></tr>
<tr><td>

[KeyRotatedError](./wasm.keyrotatederror.md)


</td><td>

Thrown when the encryption key used to create a JWE has been rotated out of the grace period and can no longer be used for decryption.


</td></tr>
<tr><td>

[RotationInProgressError](./wasm.rotationinprogresserror.md)


</td><td>

Thrown when a key rotation is requested while a previous rotation is still within its grace period (i.e. the previous key has not yet been retired).


</td></tr>
<tr><td>

[SecretNotFoundError](./wasm.secretnotfounderror.md)


</td><td>

Thrown when a requested secret does not exist in the backend store.


</td></tr>
<tr><td>

[TokenExpiredError](./wasm.tokenexpirederror.md)


</td><td>

Thrown when a JWE token has passed its expiration time (`exp` claim).


</td></tr>
<tr><td>

[TokenRevokedError](./wasm.tokenrevokederror.md)


</td><td>

Thrown when a JWE token has been explicitly blocked (e.g. after a single-use token has already been consumed).


</td></tr>
<tr><td>

[UsageLimitExceededError](./wasm.usagelimitexceedederror.md)


</td><td>

Thrown when a token with a finite `use` limit has been presented more times than the limit allows.


</td></tr>
<tr><td>

[VaultError](./wasm.vaulterror.md)


</td><td>

Base error for all `@vaultkeeper/wasm` errors.


</td></tr>
<tr><td>

[VaultKeeper](./wasm.vaultkeeper.md)


</td><td>

A VaultKeeper instance backed by Rust/WASM.

Provides the same API as the pure TypeScript VaultKeeper but with the Rust core handling all crypto, token lifecycle, and business logic.


</td></tr>
</tbody></table>

## Functions

<table><thead><tr><th>

Function


</th><th>

Description


</th></tr></thead>
<tbody><tr><td>

[createNodeHost(configDirOverride)](./wasm.createnodehost.md)


</td><td>

Create a Node.js host platform for the WASM module.

Uses the standard vaultkeeper config directory: - macOS/Linux: `~/.config/vaultkeeper` - Windows: `%APPDATA%/vaultkeeper`

Override with `VAULTKEEPER_CONFIG_DIR` environment variable.


</td></tr>
<tr><td>

[createVaultKeeper(options, configDir)](./wasm.createvaultkeeper.md)


</td><td>

Convenience function to create a VaultKeeper instance.

Equivalent to `VaultKeeper.create(options, configDir)`<!-- -->.


</td></tr>
</tbody></table>

## Interfaces

<table><thead><tr><th>

Interface


</th><th>

Description


</th></tr></thead>
<tbody><tr><td>

[AuthorizeResult](./wasm.authorizeresult.md)


</td><td>

Authorization result. Combines the validated claims (secret redacted), the response metadata, and a one-time [SecretAccessor](./wasm.secretaccessor.md) for the plaintext secret.


</td></tr>
<tr><td>

[PreflightCheck](./wasm.preflightcheck.md)


</td><td>

Individual preflight check result. The Rust struct serializes with `#[serde(rename_all = "camelCase")]`<!-- -->, so the wire/JSON field names here are camelCase (matching this interface), not the Rust source's snake\_case.

`required` reflects whether this dependency is required for the active/configured backend(s); plugin-backend checks (`op`<!-- -->, `ykman`<!-- -->) are `required: false` when their backend isn't enabled (issue \#116).


</td></tr>
<tr><td>

[PreflightResult](./wasm.preflightresult.md)


</td><td>

Overall preflight result.


</td></tr>
<tr><td>

[SecretAccessor](./wasm.secretaccessor.md)


</td><td>

A one-time accessor for the raw secret produced by `authorize()`<!-- -->.

Mirrors the `getSecret()` pattern in the TypeScript library: the plaintext secret is exposed only through a single `read()` callback and cannot be read a second time. This keeps the secret out of the default return shape and confines its lifetime to the callback.


</td></tr>
<tr><td>

[SetupOptions](./wasm.setupoptions.md)


</td><td>

Options for the setup (token creation) operation.


</td></tr>
<tr><td>

[VaultClaims](./wasm.vaultclaims.md)


</td><td>

Claims embedded in a JWE token, as returned by [AuthorizeResult](./wasm.authorizeresult.md)<!-- -->.

The raw secret value (`val`<!-- -->) is deliberately \*\*absent\*\* — it is never part of `authorize()`<!-- -->'s return shape. Read the secret through [SecretAccessor](./wasm.secretaccessor.md) instead.


</td></tr>
<tr><td>

[VaultConfig](./wasm.vaultconfig.md)


</td><td>

Vault configuration.


</td></tr>
<tr><td>

[VaultKeeperOptions](./wasm.vaultkeeperoptions.md)


</td><td>

Options for creating a WasmVaultKeeper instance.


</td></tr>
<tr><td>

[VaultResponse](./wasm.vaultresponse.md)


</td><td>

Response from token authorization.


</td></tr>
<tr><td>

[WasmHostPlatform](./wasm.wasmhostplatform.md)


</td><td>

Host platform interface that bridges Node.js OS calls to the WASM module.

Implementations of this interface are passed to the WASM VaultKeeper constructor to provide file I/O and subprocess execution.


</td></tr>
</tbody></table>

## Type Aliases

<table><thead><tr><th>

Type Alias


</th><th>

Description


</th></tr></thead>
<tbody><tr><td>

[KeyStatus](./wasm.keystatus.md)


</td><td>

Key status in the vault response.


</td></tr>
<tr><td>

[PreflightCheckStatus](./wasm.preflightcheckstatus.md)


</td><td>

Preflight check status (Rust kebab-case serialization).


</td></tr>
<tr><td>

[TrustTier](./wasm.trusttier.md)


</td><td>

Trust tier classification.


</td></tr>
</tbody></table>

