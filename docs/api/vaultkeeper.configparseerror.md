<!-- Do not edit this file. It is automatically generated by API Documenter. -->

[Home](./index.md) &gt; [vaultkeeper](./vaultkeeper.md) &gt; [ConfigParseError](./vaultkeeper.configparseerror.md)

## ConfigParseError class

Thrown when a config file's contents cannot be parsed as JSON.

The `message` already embeds the file path, the parse location (when the underlying `SyntaxError` exposes one), and a remediation hint that either points at `vaultkeeper config init` (via the separate `@vaultkeeper/cli` package — this library ships no CLI of its own) or at repairing/replacing the config directly through this library's JS API — see issues \#68, \#100. `path` and `location` are also exposed individually for callers (e.g. `doctor`<!-- -->) that want to report them as structured fields rather than re-parsing the message.

**Signature:**

```typescript
declare class ConfigParseError extends VaultError 
```
**Extends:** [VaultError](./vaultkeeper.vaulterror.md)

## Constructors

<table><thead><tr><th>

Constructor


</th><th>

Modifiers


</th><th>

Description


</th></tr></thead>
<tbody><tr><td>

[(constructor)(message, path, location)](./vaultkeeper.configparseerror._constructor_.md)


</td><td>


</td><td>

Constructs a new instance of the `ConfigParseError` class


</td></tr>
</tbody></table>

## Properties

<table><thead><tr><th>

Property


</th><th>

Modifiers


</th><th>

Type


</th><th>

Description


</th></tr></thead>
<tbody><tr><td>

[location](./vaultkeeper.configparseerror.location.md)


</td><td>

`readonly`


</td><td>

string \| undefined


</td><td>

A human-readable parse location (e.g. `'line 3, column 12'`<!-- -->), when one could be derived from the underlying `SyntaxError`<!-- -->.


</td></tr>
<tr><td>

[path](./vaultkeeper.configparseerror.path.md)


</td><td>

`readonly`


</td><td>

string


</td><td>

The path of the config file that failed to parse. This is `configDir` joined with `config.json` exactly as provided to `loadConfig` — it is not guaranteed to be absolute (`loadConfig` does not resolve a relative `configDir`<!-- -->).


</td></tr>
</tbody></table>

