<!-- Do not edit this file. It is automatically generated by API Documenter. -->

[Home](./index.md) &gt; [@vaultkeeper/wasm](./wasm.md) &gt; [FilesystemError](./wasm.filesystemerror.md)

## FilesystemError class

Thrown when a filesystem operation fails while reading, writing, or deleting a stored secret entry. Common causes include a permission or access problem, but the underlying failure may be any OS errno condition — inspect [FilesystemError.code](./wasm.filesystemerror.code.md) for the specific errno when one is available. Mirrors the pure-TypeScript `vaultkeeper` library's `FilesystemError` field names (`path`<!-- -->, `permission`<!-- -->, `code`<!-- -->).

**Signature:**

```typescript
export declare class FilesystemError extends VaultError 
```
**Extends:** [VaultError](./wasm.vaulterror.md)

## Constructors

<table><thead><tr><th>

Constructor


</th><th>

Modifiers


</th><th>

Description


</th></tr></thead>
<tbody><tr><td>

[(constructor)(message, path, permission, code)](./wasm.filesystemerror._constructor_.md)


</td><td>


</td><td>

Constructs a new instance of the `FilesystemError` class


</td></tr>
</tbody></table>

## Properties

<table><thead><tr><th>

Property


</th><th>

Modifiers


</th><th>

Type


</th><th>

Description


</th></tr></thead>
<tbody><tr><td>

[code](./wasm.filesystemerror.code.md)


</td><td>

`readonly`


</td><td>

string \| undefined


</td><td>

The underlying OS errno code (e.g. `'ENOENT'`<!-- -->, `'EACCES'`<!-- -->), when the Node host bridge was able to supply one. `undefined` when no code was available — never fabricated.


</td></tr>
<tr><td>

[path?](./wasm.filesystemerror.path.md)


</td><td>

`readonly`


</td><td>

string


</td><td>

_(Optional)_ The path that caused the error. In practice the Rust core always supplies this for `VaultError::Filesystem`<!-- -->, so `undefined` only occurs if a malformed boundary shape omitted it — never fabricated as an empty string, so its absence is distinguishable from a genuine path.


</td></tr>
<tr><td>

[permission?](./wasm.filesystemerror.permission.md)


</td><td>

`readonly`


</td><td>

string


</td><td>

_(Optional)_ The file operation that was being attempted, e.g. `'read'` or `'write'` — the WASM host bridge reports a delete as `'write'`<!-- -->, mirroring the native CLI host's classification (crates/vaultkeeper-cli/src/host.rs). Despite the field name, this does not imply the failure was itself a permission problem: it names the attempted operation regardless of the underlying errno, which may be a non-permission code. As with `path`<!-- -->, this is always populated by the real core; `undefined` only guards a malformed boundary shape.


</td></tr>
</tbody></table>

