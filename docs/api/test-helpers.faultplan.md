<!-- Do not edit this file. It is automatically generated by API Documenter. -->

[Home](./index.md) &gt; [@vaultkeeper/test-helpers](./test-helpers.md) &gt; [FaultPlan](./test-helpers.faultplan.md)

## FaultPlan class

A small, backend-agnostic scripting surface for deterministic, per-operation fault injection: arm a [FaultMode](./test-helpers.faultmode.md) against an operation key (one-shot or persistent), then consult it before doing real work.

**Signature:**

```typescript
declare class FaultPlan<Operation extends string = string> 
```

## Remarks

`FaultPlan` knows nothing about any particular backend's storage or resource namespaces — it only tracks "which operation key has which mode armed, and for how long." A test double (e.g. `InMemoryBackend`<!-- -->, or a future backend-flavored double faking a CLI-backed store) holds one instance per double, consults [FaultPlan.consume()](./test-helpers.faultplan.consume.md) at the top of each operation, and is responsible for turning the returned [FaultMode](./test-helpers.faultmode.md) into the concrete typed `vaultkeeper` error class appropriate for that operation's semantics.

`Operation` is a type parameter so each double can key faults by its own operation vocabulary (e.g. `InMemoryBackend`<!-- -->'s `store`<!-- -->/`retrieve`<!-- -->/`sign`<!-- -->-ish method names, or a future double's CLI subcommand names) while sharing this same scripting mechanism.

## Methods

<table><thead><tr><th>

Method


</th><th>

Modifiers


</th><th>

Description


</th></tr></thead>
<tbody><tr><td>

[clear(operation)](./test-helpers.faultplan.clear.md)


</td><td>


</td><td>

Remove an armed fault for `operation`<!-- -->, if any. Safe to call when no fault is armed.


</td></tr>
<tr><td>

[clearAll()](./test-helpers.faultplan.clearall.md)


</td><td>


</td><td>

Remove all armed faults, regardless of mode or operation.


</td></tr>
<tr><td>

[consume(operation)](./test-helpers.faultplan.consume.md)


</td><td>


</td><td>

Consult the plan for `operation`<!-- -->. If a fault is armed, a one-shot entry is consumed (removed) before returning; a persistent entry is left armed. Returns `undefined` when no fault is armed for `operation`<!-- -->.


</td></tr>
<tr><td>

[inject(operation, mode, options)](./test-helpers.faultplan.inject.md)


</td><td>


</td><td>

Arm a fault for the next matching call to `operation`<!-- -->.


</td></tr>
</tbody></table>

