<!-- Do not edit this file. It is automatically generated by API Documenter. -->

[Home](./index.md) &gt; [@vaultkeeper/test-helpers](./test-helpers.md) &gt; [FaultMode](./test-helpers.faultmode.md)

## FaultMode type

The deterministic fault modes a [FaultPlan](./test-helpers.faultplan.md) can script. Each is a backend-agnostic scenario name — mapping a mode to a concrete typed error class is the consulting double's responsibility (see e.g. `InMemoryBackend.injectFault`<!-- -->), since the right class can depend on backend-specific context (which operation, which resource namespace).

**Signature:**

```typescript
type FaultMode = 'backend-unavailable' | 'key-absent' | 'permission-denied' | 'session-expired';
```

## Remarks

This vocabulary is intentionally shared across every test double in this package, in-memory or backend-flavored (e.g. a stub CLI process faking `op`<!-- -->/`ykman`<!-- -->/`security` on `PATH`<!-- -->): a consumer's fault-handling test should read the same regardless of which double it targets.

