---
"@vaultkeeper/cli": minor
---

Add `@vaultkeeper/cli` package — command-line interface for vaultkeeper secret management. Provides `vaultkeeper exec` for injecting secrets as environment variables with output redaction, plus commands for doctor checks, secret storage, key rotation, and configuration management.
