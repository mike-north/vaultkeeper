---
"vaultkeeper": minor
---

Add ExecError, InvalidTokenError, and AccessorConsumedError for precise error handling; validate secret names are non-empty; detect {{secret}} in exec command field; update README quick start with store() step
